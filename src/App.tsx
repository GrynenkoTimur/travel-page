import { LocationPage } from "./pages";

function App() {
  // here should be a router if there were more than 1 page, however for this test task there is no need in that
  return <LocationPage />;
}

export default App;
